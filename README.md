# UVM

Stand for Universal Version Manager is a library that aim to allow for an easier and greater version control of your packages.
It aims to allow for automatic package generation based on git diff in a git directory and allows for automatic upgrade.

# TODO

- Update UML documentation.
- Add unit tests for all UVM functionalities.
- Upgrade UVM in order to take into account multiple configuration and customize them. (make a UVM4Cs_V2)
